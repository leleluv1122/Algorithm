#include <iostream>
#include <string>

#define endl "\n"
using namespace std;

int N, M;
string S;

void init() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
}

int main() {
	init();

	cin >> N >> M;
	cin >> S;
}