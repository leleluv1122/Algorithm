#include <string>
#include <vector>

using namespace std;

int solution(vector<int> cards) {
	vector<int> player;
	int psum = 0, dsum = 0;
	vector<int> diller;

	for (int i = 0; i < cards.size(); i++) {
		
	}
}